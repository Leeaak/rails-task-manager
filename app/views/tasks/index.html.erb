<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <li><%= task.details %></li>
  <% end %>
</ul>

<%= link_to 'New task', tasks_new_path %>
